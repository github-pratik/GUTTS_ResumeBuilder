{
  "name": "gutts-resume-builder-mcp",
  "version": "1.0.0",
  "description": "Remote MCP server that drives the live GUTTS Resume Builder website (https://gutts-resumebuilder.onrender.com) via a headless browser, so Claude can build, edit, and export student resumes end-to-end.",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.19.1",
    "express": "^4.21.2",
    "playwright": "^1.49.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
